PORT=3000
DATABASE_URL=./data/clalan.sqlite
JWT_SECRET=replace-this-with-a-long-random-secret
# Comma-separated frontend origins, without trailing slashes. Use * for a
# frontend served from the same VPS under changing ports or a reverse proxy.
CLIENT_ORIGIN=*
